public boolean hasBad(String str) {
  if (str.length() < 3) {
            return false;
        }
        return str.startsWith("bad") || (str.length() > 3 && str.substring(1, 4).equals("bad"));
    }
